<img width="1024" height="576" alt="image" src="https://github.com/user-attachments/assets/9887120d-797a-4713-a16b-43f547c17cd7" />
# Climate Data Analysis

This project looks at how temperature, CO₂ emissions, and precipitation have changed across the world from 1940 to 2024. The data comes from three Our World in Data CSV files, which I cleaned and merged into one dataset covering 178 countries. From there I made charts to explore the trends, then trained two machine learning models to predict temperature and precipitation.

---

## What's in the notebook

**Data cleaning and merging**
The three datasets all use slightly different column names and formats, so the first part of the notebook standardises everything and joins them on country + year. Rows with missing values are dropped rather than filled in, since guessing climate data felt wrong.

**Visualisations**
- Global temperature, CO₂, and precipitation trends over time
- Hottest and coldest countries, biggest and smallest emitters, wettest and driest countries
- A correlation heatmap showing how all the variables relate to each other
- CO₂ over time for the top 10 emitters (you can really see China's rise and the 2020 COVID dip)
- All three variables broken down by continent

**Machine learning**
Two models — Linear Regression and Random Forest — both trained to predict temperature and precipitation. Country is one-hot encoded so the model knows where each row is located geographically. Both models end up with very high R² scores (~0.99 for temperature), mostly because knowing which country a row belongs to already tells you most of what you need to know about its climate.

---

## Files

```
AI PROJECT/
├── climate_project.ipynb
├── average-monthly-surface-temperature.csv
├── owid-co2-data.csv
├── average-precipitation-per-year.csv
├── merged_climate_data.csv        (generated by the notebook)
└── README.md
```

---

## How to run it

Install the dependencies if you haven't already:

```bash
pip install pandas matplotlib seaborn scikit-learn jupyter
```

Then open the notebook:

```bash
jupyter notebook climate_project.ipynb
```

Run the cells top to bottom — some cells depend on variables from earlier ones, so running them out of order will cause errors. In Jupyter you can do **Kernel → Restart & Run All** to run everything at once.

---

## Results

| Model | Target | R² |
|---|---|---|
| Linear Regression | Temperature | 0.996 |
| Linear Regression | Precipitation | 0.941 |
| Random Forest | Temperature | 0.985 |
| Random Forest | Precipitation | 0.941 |

Linear Regression actually edges out Random Forest on temperature, which was a bit surprising. For precipitation both models perform about the same.
